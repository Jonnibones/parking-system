on: 
  push:
    branches:
    - main
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:

        server: ftp://systemtesting77.shop
        username: u248333124
        password: ${{ secrets.FTP_PASS }}  
        server-dir: public_html/parking-system/
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**   
          
